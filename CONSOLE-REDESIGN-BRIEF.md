# إعادة بناء «لوحة المالك» — بريف تصميم

> انسخ هذا الملفّ كاملاً وأرسله. وهو مكتوبٌ ليُقرأ مرّةً واحدة ويُعمل به.

---

## ٠. المطلوب في سطر

أعِد بناء واجهة **لوحة مالك** في منتجٍ عربيّ (RTL) اسمه «النبضة»: الصفحات نفسها،
والمعمار نفسه (شريطٌ رئيسيّ جانبيّ + شريطٌ فرعيّ جانبيّ + محتوى)، **ولغةٌ بصرية
جديدة بالكامل** — لا ترقيعَ ما هو قائم.

وقبل أن تكتب سطرَ كود: **ابحث بصرياً بحثاً عميقاً**، واعرض عليّ ثلاثة اتجاهات.

---

## ١. المنتج — لتفهم روحه لا شكله

«النبضة» لعبةُ مسابقاتٍ جماعية تُلعب في قاعة: فريقٌ على شاشة عرض، ولاعبون على
جوّالاتهم، ومنظّمٌ يدير الجولة، ومالكٌ يراقب كل شيء من لوحته.

**الاستعارة الحاكمة: جهاز مراقبة قلب (ECG).** لكل فريقٍ «مجرى» يجري فيه نبضٌ من
اليمين إلى اليسار؛ الإجابة الصحيحة تزيد الوقت فتزيد النبضات، والخطأ ينقصه حتى
يستقيم الخطّ — «flatline» — فيخرج الفريق. هذه الاستعارة موجودةٌ في الشاشات
الثلاث الأخرى، ولونُ الهوية مشتقٌّ منها (سماويّ الأجهزة الطبية).

**اللوحة ليست شاشة لعب.** هي مكتبُ المالك: يفتحها على لابتوب، على مهل، ليعرف حال
البنوك والغرف والبلاغات. تحتمل الكثافة، وتُقاس بكم يُعرف منها بنظرة لا بكم تريح
العين. لكنها يجب أن تبدو **من عائلة** الشاشات الأخرى، لا برنامجاً آخر.

**اللغة عربية بالكامل، والاتجاه RTL، ومن اليوم الأول لا كترجمةٍ لاحقة.**

---

## ٢. المعمار الذي يبقى (لا تغيّره)

ثلاث نطاقاتٍ رأسية من اليمين إلى اليسار:

```
┌──────────┬───────────────┬──────────────────────────────────┐
│ المحتوى                  │ الشريط الفرعيّ │ الرفّ  │
│ ترويسة: اسم الصفحة +     │ اسم الباب       │ شعار  │
│ سطرُها + بحث + أفعال      │ مجموعاتٌ معنونة  │ أيقونة│
│ ───────────────────────  │  • الصفحات      │ أيقونة│
│ أربعُ بلاطاتٍ رقمية        │  • البنوك       │ أيقونة│
│ ألواحُ محتوى (جداول…)    │  • التصفية      │ أيقونة│
│                          │ كلُّ صفٍّ بعدده   │ خروج  │
└──────────┴───────────────┴──────────────────────────────────┘
```

- **الرفّ** (~64px): أربعة أبواب — اللوحة · البنوك · الغرف · التعليقات — وخروجٌ
  أسفله. المختار يُعرف بشاهدٍ **ينزلق** بين الأيقونات لا بلونٍ يُطفأ ويُشعل.
  واسم الأيقونة يظهر عند المرور (إلى يسارها؛ الرفّ ملتصقٌ بالحافّة اليمنى).
- **الشريط الفرعيّ** (~264px): الصفحات الفرعية، ومنتقي البنك، وخيارات التصفية —
  كلُّها صفوفٌ رأسية بأعدادها. وهو للتصفية لا للمحتوى: يبقى ثابتاً وأنت تتنقّل.
  يُطوى ويُفتح، ويصير دُرجاً طافياً تحت ١٠٢٤px.
- **المحتوى**: ترويسة لاصقة فيها اسم الصفحة الفرعية + سطرٌ يصفها + بحث + أفعال
  الصفحة (مثل «+ سؤال جديد» و«تحديث»)، ثم المتن بعرضٍ أقصاه ~١٢٨٠px.

**ولكل صفحةٍ الإيقاع نفسه:** أربع بلاطاتٍ رقمية في الأعلى (أولاها مصبوغةٌ بلون
الهوية)، ثم ألواح المحتوى. ثباتُ هذا الموضع من صفحةٍ إلى صفحة هو ما يجعلها
تبدو منتجاً واحداً — وقد اشتكى المالك صراحةً من تغيّر المقاسات بين الصفحات.

---

## ٣. الصفحات وبياناتها الحقيقية

الأرقام أدناه حقيقية من قاعدة العمل — صمّم عليها لا على بياناتٍ مخترعة.

### ٣.١ اللوحة (الرئيسية)

- تحيّة: تاريخ هجريّ (`Intl` بتقويم `islamic-umalqura`) + «مساء الخير» + سطرُ حال
  («٣ غرف قائمة الآن · ١١ بلاغاً على الأسئلة»).
- أربع بلاطات: أسئلة البنوك (١٠٥٤) · الغرف (٩) · اللاعبون (٢٥) · البلاغات (١١).
- **نداءُ عمل** واحد: لوحٌ ملوّن يظهر فقط إن كان ثمّة ما يُفعل (بلاغات تنتظر /
  فحصٌ وجد أخطاء / أسئلة ضعيفة)، ومعه زرٌّ يذهب إليه.
- مخطّط نشاطٍ لأربعة عشر يوماً (أعمدة؛ مرسومٌ يدوياً بلا مكتبة).
- «الأولى بالنظر»: ستّة أسئلة اجتمع فيها ضعفُ الصواب وكثرةُ البلاغ.
- جدول البنوك: البنك · سهل · متوسط · صعب · الأسئلة · عُرض · نسبة الصواب · بلاغات
  + صفُّ مجموع. (طُلب صراحةً: **أعداد لا أشرطة ملوّنة**.)
- آخر الغرف · آخر التعليقات.

### ٣.٢ البنوك — خمس صفحات

| الصفحة | المحتوى | الحجم |
|---|---|---|
| الأسئلة | قائمة: نقطةُ مستوى · نصّ السؤال · خياراته · البنك · عُرض · نسبة الصواب. تُضغط فتفتح المحرّر | **١٠٥٤ صفّاً في صفحة واحدة** |
| صحّة الأسئلة | جدول: السؤال · البنك · عُرض · صح · خطأ · النسبة · بلاغ | ٢٨٣ |
| بلاغات الأسئلة | بلاغاتٌ **مجموعةٌ بالسؤال**، الأكثرُ بلاغاً أولاً، ولكلٍّ سببُه ونصُّه وغرفتُه وصفةُ المبلِّغ (بلا هوية) | ١١ في ٨ أسئلة |
| الأسئلة المحرَّرة | أرشيفُ ٣٠ يوماً: القديم والجديد جنباً إلى جنب + ما مُحي من إحصاء + زرُّ إرجاع | يتغيّر |
| — | منتقي البنك في الشريط الفرعيّ: ١١ بنكاً بأعدادها (٧٥–١٢٣) | |

**تنبيه أداء:** صفحة الأسئلة تعرض ١٠٥٤ صفّاً حقيقياً دفعةً واحدة، والتبديل بين
البنوك فرزٌ في المتصفّح بلا نداءٍ جديد. أيُّ تصميمٍ تقترحه يجب أن يصمد عند هذا
العدد (`content-visibility` مستعملٌ الآن).

### ٣.٣ الغرف

جدولٌ من تسعة أعمدة: الرمز · الاسم · التاريخ · المستوى · لاعبون · جولات · أسئلة ·
المدّة · الحال. والمدّة تُحسب من انطلاق أول جولة إلى آخر أثرٍ في الغرفة.

### ٣.٤ التعليقات

بطاقات: نجماتٌ (١–٥) · صفةُ صاحبه (منظّم/لاعب باسم فريقه) · الغرفة · الوقت · النصّ.

### ٣.٥ المحرّر (نافذة)

نافذةٌ في وسط الشاشة وما خلفها مُعتَم: نصّ السؤال · أربعة خيارات (الأول هو الصواب
ويُميَّز) · المستوى (ثلاثة) · **فحصٌ فوريّ وأنت تكتب** يُظهر أخطاءً تمنع الحفظ
وتنبيهاتٍ تمرّ · شريطُ إحصاءٍ للسؤال · حذفٌ بخطوتين.

### ٣.٦ باب الدخول

صفحةٌ واحدة: حقلُ مفتاحٍ سرّيّ وزرّ. هي أول ما يُرى من اللوحة — فاجعلها من عالمها.

---

## ٤. القيود التقنية (غير قابلة للتفاوض)

- **React 19 + TypeScript + Tailwind v4 + Vite.** الرموز في `@theme static`،
  ورموزٌ **دلالية** لا حرفية (`--color-signal` لا `--color-cyan`).
- **لا مكتبة جديدة بلا إذن.** لا shadcn ولا Radix ولا framer-motion ولا رسّام
  مخطّطات. الأيقونات مرسومةٌ في `icons.tsx` (SVG بخطٍّ موحّد). المخطّطات بالـCSS.
- **خطٌّ واحد: Thmanyah** (محلّيّ، خمسة أوزان: 300/400/500/700/900). لا خطوط
  Google. اشتغل على الوزن والحجم والتباعد لا على تنويع الخطوط.
- **RTL صحيحٌ بالخصائص المنطقية** (`ms/me/ps/pe/start/end`) لا بـ`left/right`.
- **تباين AA** على كل نصّ. لون الهوية على سطحه الفاتح يعطي ٣.٧:١ — فلا يُستعمل
  نصّاً إلا بنسخةٍ داكنة (`--color-signal-ink`). القاعدة نفسها للحالات.
- **`prefers-reduced-motion`** يُحترم في كل حركة.
- **٦٠ إطاراً** — الحركة بـ`transform`/`opacity` لا غير.
- الوضع الفاتح هو وضع اللوحة (الشاشات الأخرى لها وضعٌ غامق؛ النظام يدعم الاثنين).
- **لا تغيّر عقد الـAPI** (أدناه). التصميم يُبنى على البيانات الموجودة.

### عقد البيانات (كما هو اليوم)

```
GET  /api/console/dashboard          → { audit{errors,warnings,duplicates},
                                         totals{rooms,players,rounds,questions,live,bank,
                                                reports,comments,stars,medianPlayedMs,
                                                measured,edits,weak,lowStars},
                                         days[{at,rooms,players}],
                                         banks[{id,name,total,levels[3],seen,reports,rate}],
                                         worst[6], recentRooms[5], recentComments[4] }
GET  /api/console/bank/all|:id       → { questions[{id,bank,bankName,q,options[4],level,
                                                    shown,rate,reports,issues[]}] }
GET  /api/console/health?minShown&limit
GET  /api/console/feedback?kind=report|comment
GET  /api/console/rooms
GET  /api/console/edits
GET  /api/console/question/:id       → السؤال + إحصاؤه + بلاغاته + ملاحظات الفاحص
POST /api/console/check              → فحصٌ فوريّ لسؤالٍ قيد الكتابة
POST /api/console/bank/:id/question · PUT|DELETE /api/console/question/:id
POST /api/console/edit/:id/restore
```

---

## ٥. أولاً: ابحث بصرياً (هذا أهمّ ما في الطلب)

**لا تبدأ من ذاكرتك.** اذهب وانظر، ثم اعرض ما رأيت.

ادرس — بالصور لا بالوصف — واجهاتِ لوحاتٍ إداريةٍ **حديثة** (٢٠٢٤ فما بعد)، ومنها
هذه الأصناف:

1. **أدواتُ فرقٍ حديثة**: Linear · Vercel · Raycast · Height · Attio · Campsite ·
   Cron/Notion Calendar. (انظر: كثافةٌ بلا ضجيج، سرعةٌ محسوسة، تراتبٌ بالحبر لا
   بالحدود.)
2. **لوحاتُ بيانات**: Stripe · Supabase · PostHog · Resend · Polar. (انظر: كيف
   تُعرض الأرقام والجداول الطويلة والحالات الفارغة.)
3. **واجهاتٌ عربية ممتازة**: ثمانية · سلّة · فودكس · تمارا · رصيد · لوحات مطاعم
   ومدارس عربية. (انظر: كيف يُعامَل الخطّ العربي في الكثافة العالية، وكيف تُرقَّم
   الجداول، وأين تقع الأفعال في RTL.)
4. **لوحاتٌ تعليمية/حلقات** (أقرب شبهاً بحالتنا): أنظمة إدارة حلقات وطلاب عربية.

**استخرج من بحثك، مكتوباً:**
- ماذا يفعل الحديثُ اليوم في: التباعد · نصف القطر · الظلال · الحدود · حجم الخطّ ·
  عرض الصفوف · موضع الأفعال · رؤوس الجداول · الشارات · الحالات الفارغة.
- **ثلاث لقطاتٍ مرجعية** على الأقل لكل اتجاهٍ تقترحه، وقُل ما الذي أخذتَه من كلٍّ
  ولماذا يناسب لوحةً عربيةً كثيفة.

---

## ٦. ما يُمنع (أخطاءٌ وقعنا فيها أو نراها كل يوم)

**بصرياً:**
- تدرّجات بنفسجية/نيلية، وكراتٌ ضبابية طافية، وglassmorphism.
- بطاقاتٌ بيضاء مكرّرة على رماديّ بلا تراتب — «كلُّ شيءٍ بطاقة».
- ظلالٌ ثقيلة، وحدودٌ حول كل شيء، وزوايا حادّة (نصفُ القطر اليوم ١٤px للألواح
  و٨px للشارات — لا تعد إلى ٤px).
- إيموجي مكان الأيقونات.
- توسيطُ كل شيء افتراضياً.
- حركةٌ زخرفية أثناء العمل.
- أشرطةٌ ملوّنة مكان الأرقام (طُلب صراحةً: اكتب العدد).
- جداولُ بلا رؤوس، وأرقامٌ في آخر السطر لا يُعرف ما هي.

**عربياً وتقنياً (أخطاءٌ كلّفتنا وقتاً فعلاً):**
- `end-full` في RTL يدفع العنصر **يميناً** خارج الشاشة؛ الصحيح `start-full`.
- ارتفاعٌ بنسبةٍ مئوية على أبٍ ارتفاعُه تلقائيّ = صفر (سقط مخطّطٌ كامل بسببه).
- خلطُ الأرقام العربية-الهندية واللاتينية في شاشةٍ واحدة.
- «٨ غرفة» و«١٠ بلاغاً» — صيغةُ المعدود تتبع العدد (`Intl.PluralRules`).
- عنصرٌ يُنتزع من مجرى التخطيط (`position:absolute`) فينكمش أبوه المرن.
- شريط تمرير النظام الرماديّ يقتطع من عرض الصفحة (البديل الحالي: خيطٌ طافٍ في
  الحافّة اليسرى، شفّاف، يُسحَب باليد، ولا يأخذ عرضاً).

---

## ٧. ما أريده منك — بهذا الترتيب

**المرحلة ١ — بحثٌ واتجاهات (قبل أيّ كود):**
- خلاصةُ بحثك البصريّ كما في §٥.
- **ثلاثة اتجاهات** مختلفة اختلافاً حقيقياً (لا ثلاث درجاتٍ من الرماديّ نفسه)،
  لكلٍّ منها: اسمٌ وجملةُ موقف · لوحةُ ألوان (٦–٨ رموز دلالية بقيمها) · مقياسٌ
  طباعيّ · مقاسُ الشبكة والتباعد ونصف القطر · **لقطةٌ واحدة** لشاشة «اللوحة
  الرئيسية» بكلٍّ منها.
- ولكلٍّ: لماذا يخدم مالكاً عربياً يقرأ ألف صفّ.

**المرحلة ٢ — بعد اختياري:**
- نظامُ الرموز كاملاً في `@theme static` (فاتح + غامق).
- مكتبةُ قطعٍ مشتركة: بلاطةُ رقم · لوحُ قسم · جدول · صفُّ قائمة · شارة · نافذة ·
  قائمة منسدلة · حقول · حالةٌ فارغة · حالةُ تحميل · شريطُ تمرير طافٍ.
- الصفحات الستّ كاملةً بحالاتها (فارغ · يُقرأ · خطأ · كثيف).
- مواصفةُ حركة: مدّة ومنحنى لكل حالة (تبديل صفحة · فتح نافذة · انزلاق الشاهد).
- **كودٌ حقيقي** يعمل مع الـAPI أعلاه: `.tsx` + Tailwind v4، بأسماء ملفاتٍ مطابقة
  لما هو قائم (انظر §٨)، و`npx tsc -b` نظيف.

---

## ٨. أين يقع الكود اليوم

```
client/src/
  index.css                     الرموز + الطبقات المشتركة
  components/ui.tsx             الذرّات (زر · حقل · قائمة منسدلة · شريط تمرير)
  components/icons.tsx          كل الأيقونات (SVG، خطٌّ موحّد)
  pages/Owner.tsx               الهيكل: الرفّ + الشريط الفرعيّ + الترويسة + التوجيه
  pages/console/shared.tsx      القطع المشتركة + النافذة + محرّر السؤال
  pages/console/Dashboard.tsx   اللوحة الرئيسية
  pages/console/Banks.tsx       الأسئلة · الصحّة · البلاغات · المحرَّرة
  pages/console/RoomsAndComments.tsx
```

استبدلها بما تراه أفضل، لكن أبقِ الحدود بين الملفّات على حالها ليبقى التتبّع سهلاً.

---

## ٩. معيار القبول

- تُفتح الصفحات الستّ كلّها، وتعمل مع الـAPI بلا تغييرٍ في السيرفر.
- المقاسات نفسها في الصفحات الستّ (بلاطة · لوح · خليّة · صفّ).
- `npx tsc -b` نظيف، و`npx oxlint src` بلا أخطاء، والبناء يمرّ.
- ألفُ صفٍّ تُمرَّر بسلاسة على لابتوب متواضع.
- كلُّ نصٍّ ≥ ٤.٥:١، وكلُّ حركةٍ تحترم `prefers-reduced-motion`.
- لا مكتبة جديدة، ولا خطٌّ جديد، ولا `left/right` في RTL.
- **ولا تبدو كأيّ لوحةٍ رأيتها مئة مرّة.** إن لم يكن فيها رأيٌ واضح فهي لم تُصمَّم.
