# بريف التصميم — «النبضة»

> **كيف تستعمله:** افتح جلسة جديدة في كلود كود داخل هذا المجلد واكتب:
> `اقرأ DESIGN-BRIEF.md كاملاً ونفّذه. ابدأ بمرحلة الاستكشاف ولا تكتب كوداً قبل أن أعتمد الاتجاه.`

---

## ١. المهمة في سطر

أعِد تصميم واجهات «النبضة» من الصفر لتصبح **هوية مستقلة قائمة بذاتها** — لا امتداداً لهوية نادي نبراس — بمستوى منتج حقيقي لا بمستوى قالب جاهز.

---

## ٢. دورك والمعيار

أنت **موجّه فنّي ومصمم منتج**، لا مبرمج ينفّذ طلبات. المعيار: لو عُرض هذا الموقع في مؤتمر منتجات، هل يُقال «تصميم جميل» أم يُقال «مين سوّى هذا؟». نستهدف الثانية.

علامة النجاح ليست «نظيف ومرتّب» — هذا الحد الأدنى. علامة النجاح أن **يشعر الحاضر في القاعة بشيء**: توتر، تشويق، لهفة.

---

## ٣. السياق الفيزيائي — اقرأه قبل أي قرار تصميمي

هذه ليست لوحة تحكّم يتصفّحها شخص جالس. هذه مسابقة تُقام في قاعة:

| الشاشة | الجهاز | المسافة | الحالة النفسية |
|---|---|---|---|
| `/display` | بروجكتر ١٩٢٠×١٠٨٠ | **٣–١٥ متر** | جمهور يتابع ويصيح |
| `/play` | جوال واحد **تتحلّق حوله ٤–٥ أشخاص** | ٣٠–٦٠ سم، بزاوية | ضغط، سرعة، تدافع |
| `/admin` | لابتوب للمنظّم | عادية | هدوء، تحكّم، لا أخطاء |

نتائج مباشرة تلزمك:
- على البروجكتر: **لا شيء أصغر من ٢٨px**. الفرق يجب أن تُقرأ من آخر صف.
- على الجوال: الشخص الذي يضغط ليس الشخص الذي يقرأ. الخيارات الأربعة تُقرأ **بالزاوية** ويُضغط عليها **بالإبهام في عجلة**. مساحة اللمس ≥ ٦٤px ارتفاعاً.
- الجولة ٣٠–٩٠ ثانية فقط. **أي حركة لا تحمل معلومة = ضجيج**.

---

## ٤. الهوية المطلوبة

**الاسم:** النبضة · **الاستعارة:** مخطّط نبضات القلب (ECG).

المعنى المركزي الذي يجب أن تحمله كل شاشة:

> الوقت نبض. كل إجابة صحيحة تُبقيه حياً، وكل خطأ يقرّبه من السكون. أول نبض يتوقف يُنهي الجولة على الجميع.

المخطّط الحيّ منفَّذ فعلاً في `TimeBar` (انظر `client/src/components/game.tsx` و`.ecg-track` في `index.css`): النبضات تقلّ مع الوقت حتى يستقيم الخط. **هذه الفكرة صحيحة — ابنِ عليها، لا تهدمها.** وسّعها إلى بقية الواجهة.

### ما يجب أن تُنتجه للهوية
1. **لوجو/wordmark** لـ«النبضة» يدمج خط المخطّط في الحروف — SVG متجه، يعمل على فاتح وغامق، ويُقرأ عند ١٦px وعند ٤٠٠px.
2. **لوحة ألوان جديدة كلياً** مشتقّة من الاستعارة لا من شعار نبراس. اقتراح اتجاه (لك أن تخالفه بحجّة): أرضية داكنة جداً كشاشة مراقبة طبية + لون إشارة واحد حادّ + الأخضر/الأصفر/الأحمر تبقى **وظيفية** (حالة الخطر) لا هوية.
3. **سُلّم نصّي** (type scale) صريح بأوزان خط ثمانية الخمسة المتوفّرة محلياً.
4. **لغة حركة** مكتوبة: ما الذي يتحرّك، ولماذا، وبأي إيقاع. الإيقاع الأساسي = النبض.
5. **أيقونة الموقع (favicon)** الحالية `nibras-logo.png` — استبدلها بلوجو النبضة.

---

## ٥. ما يُحذف

- شعار نادي نبراس من النافبار والفوتر (`nibras-logo.png` / `nibras-logo-white.png`).
- سطر «© نادي نبراس» و«مسابقة جماعية من تنظيم نادي نبراس».
- التعليق في `index.css` الذي يقول إن الألوان «مأخوذة من شعار نادي نبراس» — والألوان نفسها.

**يبقى:** سطر «تم إنشاء الموقع بواسطة مشعل الجلال».

---

## ٦. نظام التصميم المطلوب

ليس «ستايلات متفرّقة» بل نظام:

- **Tokens** في `@theme` داخل `client/src/index.css`: ألوان دلالية (`--color-signal`, `--color-danger`, `--color-surface`…) لا ألوان بأسمائها الحرفية.
- **سُلّم مسافات** ثابت، وسُلّم أنصاف أقطار ثابت (لا `rounded-xl` هنا و`rounded-2xl` هناك اعتباطاً).
- **مكوّنات موحّدة**: زر، بطاقة، حقل، شارة حالة — لكل منها حالات (عادي/معطّل/خطر/نجاح).
- **وضع غامق حقيقي** على الأقل لشاشة العرض (القاعة مظلمة غالباً والبروجكتر يغسل الألوان الفاتحة).
- **تباين AA** كحدّ أدنى، و**AAA للنصوص الكبيرة** على البروجكتر.

---

## ٧. الشاشات — وظيفة كل واحدة

### `/` صفحة التعريف
**وظيفتها:** تُقنع شخصاً في ٥ ثوانٍ أنه يريد اللعب.
البطل الحالي (رسم المخطّط ثم انفجار الاسم) فكرته صحيحة — **ارفع مستواه ولا تُلغِه**. بقية الصفحة (كيف تُلعب؟ / اختر مكانك) تقليدية جداً وتستحق إعادة تفكير كاملة.

### `/play` شاشة المجموعة — **الأهم**
**وظيفتها:** السؤال والخيارات الأربعة والعدّاد. لا شيء غيرها.
- العدّاد والمخطّط يجب أن يُحسّا كجهاز مراقبة على صدر المجموعة.
- الخيارات الأربعة: تدرّجاتها الحالية ثابتة بحسب الموقع (وهذا مقصود — لا تجعلها عشوائية).
- الثواني الأخيرة يجب أن تكون **مخيفة**: الإطار الأحمر الحالي بداية، اذهب أبعد.
- بطاقة نهاية الجولة + متجر البطاقات + مراجعة الأسئلة: مزدحمة حالياً، أعد ترتيبها.

### `/display` شاشة العرض — **محرّك الدراما**
**وظيفتها:** تجعل القاعة تتفاعل.
- صفوف الفرق: الترتيب، الاسم، النقاط، المخطّط الحيّ.
- لحظة توقّف أول نبض يجب أن تكون **حدثاً في القاعة**، لا تغيّر لون صفّ.
- ستارة النتائج، المدرّج النهائي، وشاشة الانتظار (QR) كلها تحتاج معالجة بمستوى «بثّ تلفزيوني».

### `/admin` لوحة المسؤول
**وظيفتها:** تحكّم سريع بلا خطأ.
- كثافة معلومات عالية، وضوح تام، والأزرار الخطرة (إعادة اللعبة/إزالة فريق) يجب أن تكون صعبة الضغط بالخطأ.
- لا تجعلها «جميلة» على حساب السرعة.

---

## ٨. الفكرة التوقيعية — مطلوبة

اختر **فكرة بصرية واحدة جريئة** تتكرّر في كل الشاشات فتصبح التوقيع الذي يُذكر به الموقع. مثال للتوضيح فقط (لا تنسخه): الواجهة كلها سطح جهاز مراقبة — خط نبض خافت يمرّ في خلفية كل شاشة وتتسارع وتيرته مع الخطر.

اقترحها بوضوح، برّرها، ثم طبّقها **بانضباط** في كل مكان. فكرة واحدة منفَّذة بإتقان أفضل من خمس أفكار متناثرة.

---

## ٩. ممنوعات صريحة

- ❌ تدرّجات بنفسجية/إنديجو، فقاعات ضبابية عائمة، glassmorphism في كل مكان — لغة «تصميم مولّد بالذكاء الاصطناعي».
- ❌ بطاقة بيضاء بحواف مدوّرة على خلفية رمادية فاتحة، مكرّرة لكل عنصر في الموقع.
- ❌ إيموجي بدل أيقونات.
- ❌ توسيط كل شيء افتراضياً.
- ❌ حركة زخرفية أثناء اللعب الفعلي. الحركة تحمل معلومة أو تُحذف.
- ❌ مكتبات جديدة بلا استئذان (لا framer-motion ولا غيرها). Tailwind v4 + CSS كافيان.
- ❌ كسر الـRTL: انتبه لـ`left-*`/`right-*` واستعمل الخصائص المنطقية حيث أمكن.

---

## ١٠. قيود تقنية صارمة

- **لا تلمس منطق اللعبة**: `server/game.js`، أسماء أحداث السوكِت، وشكل الـpayload. التصميم في `client/src` فقط (عدا إضافة أصول في `client/public`).
- `npx tsc -b` نظيف، و`npm test` أخضر (اختبار البطاقات + e2e عبر سوكِت حقيقي).
- يعمل عند عرض **٣٧٥px** وعلى **١٩٢٠×١٠٨٠**.
- الأداء: شاشة العرض قد تحمل **٨ فرق** بمخطّطات حيّة في وقت واحد — الحركة يجب أن تبقى ٦٠fps على لابتوب متواضع. فضّل `transform`/`opacity`.
- خط ثمانية بأوزانه الخمسة موجود محلياً في `client/public/fonts` — استعمله، ولا تُحمّل خطوطاً من الشبكة.
- احترم `prefers-reduced-motion`.

---

## ١١. طريقة العمل — التزم بها

1. **استكشاف**: اقرأ `client/src` كاملاً، وشغّل الموقع، وجرّب الشاشات الأربع فعلياً في المتصفح قبل أن تحكم.
2. **٣ اتجاهات فنية**: اعرض عليّ ثلاثة اتجاهات مختلفة جوهرياً (لا ثلاث درجات من نفس الفكرة)، كل واحد بـ: اسم، جملة تصف الإحساس، لوحة ألوان، معالجة نصّية، والفكرة التوقيعية. نفّذ شاشة `/display` كنموذج مرئي لكل اتجاه.
3. **قف وانتظر اعتمادي.** لا تبنِ الأربع شاشات قبل أن أختار.
4. **التنفيذ**: نظام التصميم أولاً (tokens ومكوّنات)، ثم الشاشات، شاشة شاشة.
5. **التحقّق**: بعد كل شاشة، افتحها في المتصفح والتقط لقطة، واختبرها عند ٣٧٥px وعلى مقاس البروجكتر. أرِني اللقطات.

---

## ١٢. تعريف الإنجاز

- [ ] لا أثر لهوية نبراس في أي شاشة.
- [ ] لوجو «النبضة» متجه، يعمل على فاتح وغامق، وفي الـfavicon.
- [ ] Tokens ونظام تصميم موثّق في `index.css`، ولا ألوان عشوائية مبعثرة في الـJSX.
- [ ] الفكرة التوقيعية حاضرة في الشاشات الأربع.
- [ ] `/display` تُقرأ من ١٠ أمتار، و`/play` تُستعمل بإبهام واحد.
- [ ] `tsc -b` نظيف و`npm test` أخضر.
- [ ] لقطات شاشة للأربع شاشات في وضعي جوال وبروجكتر.
- [ ] لا مكتبات جديدة.
