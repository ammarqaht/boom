{
  "name": "nabda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "نبضة — لعبة مسابقة جماعية: فرقٌ تتسابق لحل الأسئلة قبل أن يتوقف نبضها",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --watch server/index.js",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client install --include=dev && npm --prefix client run build",
    "start": "node server/index.js",
    "test": "cd client && node env-test.mjs && node rules-test.mjs && node memory-test.mjs && node cards-test.mjs && node e2e.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  }
}
